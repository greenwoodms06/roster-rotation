{
  "name": "roster-rotation-e2e",
  "private": true,
  "description": "Playwright smoke suite for Rotation Manager. Isolated from the app — not a runtime dep.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
